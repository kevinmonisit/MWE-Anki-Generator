{
  "name": "youtube-transcript-viewer",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "css": "npx tailwindcss -i src/input.css -o dist/output.css",
    "build": "npm run css && tsc",
    "start": "npm run css && tsc && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Download YouTube videos and view synced transcripts",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "electron": "^41.0.2",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.9.3"
  }
}
